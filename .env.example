# Pooled Neon connection string (Phase 2). See docs/environment-setup.md.
# Local dev without a database: run with NEXT_PUBLIC_ENABLE_MOCKS=1 instead.
DATABASE_URL=

# Set to 1 to run the app against the in-memory mock API (ADR 0005).
NEXT_PUBLIC_ENABLE_MOCKS=
